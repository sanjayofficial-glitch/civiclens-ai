/** * Display metadata for the canonical CivicLens domain enums. * * Source of truth for the lowercase enums is `@civiclens/shared` * (schemas/enums + constants). Every screen renders status / severity / * category through these maps so labels, colours and icons stay consistent. */import {  AlertTriangle,  Construction,  Droplets,  Lightbulb,  MapPin,  PaintRoller,  Sparkles,  ThumbsUp,  Trash2,  XCircle,  type LucideIcon,} from 'lucide-react';import type {  IssueCategory,  IssueSeverity,  IssueStatus,  LeaderboardPeriod,} from '@civiclens/shared';/* ----------------------------- Status ----------------------------- */export interface StatusMeta {  label: string;  /** Tailwind classes for a solid badge background */  badge: string;  /** Tailwind classes for a soft / tinted badge background */  softBadge: string;  /** Raw CSS var token name (without leading --) for inline styling */  token: string;  icon: LucideIcon;}export const STATUS_META: Record<IssueStatus, StatusMeta> = {  reported: {    label: 'Reported',    badge: 'bg-status-reported text-status-reported-foreground',    softBadge:      'bg-status-reported/12 text-status-reported ring-1 ring-inset ring-status-reported/25',    token: 'status-reported',    icon: MapPin,  },  verified: {    label: 'Verified',    badge: 'bg-status-verified text-status-verified-foreground',    softBadge:      'bg-status-verified/12 text-status-verified ring-1 ring-inset ring-status-verified/25',    token: 'status-verified',    icon: ThumbsUp,  },  in_progress: {    label: 'In Progress',    badge: 'bg-status-in_progress text-status-in_progress-foreground',    softBadge:      'bg-status-in_progress/15 text-status-in_progress ring-1 ring-inset ring-status-in_progress/30',    token: 'status-in_progress',    icon: Construction,  },  resolved: {    label: 'Resolved',    badge: 'bg-status-resolved text-status-resolved-foreground',    softBadge:      'bg-status-resolved/12 text-status-resolved ring-1 ring-inset ring-status-resolved/25',    token: 'status-resolved',    icon: Sparkles,  },  rejected: {    label: 'Rejected',    badge: 'bg-status-rejected text-status-rejected-foreground',    softBadge:      'bg-status-rejected/12 text-status-rejected ring-1 ring-inset ring-status-rejected/25',    token: 'status-rejected',    icon: XCircle,  },};export function getStatusMeta(status: IssueStatus): StatusMeta {  return STATUS_META[status];}/* ----------------------------- Severity ----------------------------- */export interface SeverityMeta {  label: string;  softBadge: string;  dot: string;  weight: number; /* for sorting / heatmap weighting */}export const SEVERITY_META: Record<IssueSeverity, SeverityMeta> = {  low: {    label: 'Low',    softBadge:      'bg-info/12 text-info ring-1 ring-inset ring-info/25',    dot: 'bg-info',    weight: 1,  },  medium: {    label: 'Medium',    softBadge:      'bg-warning/15 text-warning-foreground ring-1 ring-inset ring-warning/30',    dot: 'bg-warning',    weight: 2,  },  high: {    label: 'High',    softBadge:      'bg-destructive/12 text-destructive ring-1 ring-inset ring-destructive/25',    dot: 'bg-destructive',    weight: 3,  },  critical: {    label: 'Critical',    softBadge:      'bg-destructive text-destructive-foreground ring-1 ring-inset ring-destructive/40',    dot: 'bg-destructive',    weight: 4,  },};export function getSeverityMeta(severity: IssueSeverity): SeverityMeta {  return SEVERITY_META[severity];}/* ----------------------------- Category ----------------------------- */export interface CategoryMeta {  label: string;  icon: LucideIcon;  /** tint used for icon chips */  chip: string;}export const CATEGORY_META: Record<IssueCategory, CategoryMeta> = {  pothole: {    label: 'Pothole',    icon: Construction,    chip: 'bg-warning/15 text-warning-foreground',  },  streetlight: {    label: 'Streetlight',    icon: Lightbulb,    chip: 'bg-warning/15 text-warning-foreground',  },  water_leak: {    label: 'Water Leak',    icon: Droplets,    chip: 'bg-info/15 text-info',  },  garbage: {    label: 'Garbage',    icon: Trash2,    chip: 'bg-success/15 text-success',  },  graffiti: {    label: 'Graffiti',    icon: PaintRoller,    chip: 'bg-primary/15 text-primary',  },  sidewalk: {    label: 'Sidewalk',    icon: AlertTriangle,    chip: 'bg-warning/15 text-warning-foreground',  },  other: {    label: 'Other',    icon: MapPin,    chip: 'bg-muted text-muted-foreground',  },};export function getCategoryMeta(category: IssueCategory): CategoryMeta {  return CATEGORY_META[category];}/* ----------------------------- Leaderboard ----------------------------- */export const LEADERBOARD_PERIOD_META: Record<  LeaderboardPeriod,  { label: string; shortLabel: string }> = {  weekly: { label: 'This Week', shortLabel: 'Weekly' },  monthly: { label: 'This Month', shortLabel: 'Monthly' },  all_time: { label: 'All Time', shortLabel: 'All Time' },};